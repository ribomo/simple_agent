[project]
name = "simple-agent"
version = "0.1.0"
description = "An educational simple agent that calls OpenAI-compatible APIs."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai==2.43.0",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
